<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.Teconv" xmlns:android="http://schemas.android.com/apk/res/android">


    <application  android:theme="@style/Theme.Teconv" android:supportsRtl="true" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">
        <activity android:name="com.example.teconv.SixActivity"/>

        <activity android:name="com.example.teconv.FiveActivity"/>

        <activity android:name="com.example.teconv.FourActivity"/>

        <activity android:name="com.example.teconv.ThirdActivity"/>

        <activity android:name="com.example.teconv.SecondActivity"/>


        <activity android:name="com.example.teconv.MainActivity">


            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>
        <service android:name="com.example.teconv.Music"/>
    </application>

</manifest>

